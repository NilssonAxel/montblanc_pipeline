# Montblanc pipeline job
resources:
  jobs:
    montblanc_pipeline:
      name: montblanc_pipeline_${bundle.target}
      timeout_seconds: 7200
      email_notifications:
        on_failure:
          - ${workspace.current_user.userName}

      tasks:
        - task_key: bronze
          python_wheel_task:
            package_name: montblanc_pipeline
            entry_point: run_bronze
            parameters:
              - "--catalog"
              - "${var.catalog}"
              - "--start-date"
              - "${var.start_date}"
              - "--end-date"
              - "${var.end_date}"
              - "--end-date-active"
              - "${var.end_date_active}"
          environment_key: default

        - task_key: silver
          depends_on:
            - task_key: bronze
          python_wheel_task:
            package_name: montblanc_pipeline
            entry_point: run_silver
            parameters:
              - "--catalog"
              - "${var.catalog}"
          environment_key: default

        - task_key: dbt
          depends_on:
            - task_key: silver
          environment_key: default
          dbt_task:
            project_directory: ../montblanc_dbt
            commands:
              - "dbt deps"
              - "dbt run"
              - "dbt test"
              - "dbt source freshness"
            warehouse_id: ${var.warehouse_id}
            catalog: ${var.catalog}
            schema: gold

      environments:
        - environment_key: default
          spec:
            environment_version: "4"
            dependencies:
              - ../dist/*.whl
              - dbt-databricks
